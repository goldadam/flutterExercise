class EventDto {
  String image;
  String title;
  String date;
  String content;
  String discount;

  // 생성자 매개변수로 초기화
  EventDto(this.image, this.title, this.date, this.content, this.discount);



}